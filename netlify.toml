[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — all routes serve index.html (React Router fix)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
